
server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/specwatch}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Sharanu}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false


jwt.secret=${JWT_SECRET:specwatch_super_secret_key_minimum_32_chars_ok}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ===========================
# GITHUB
# ===========================
github.webhook.secret=${GITHUB_WEBHOOK_SECRET:specwatch123}

# ===========================
# APP
# ===========================
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
specwatch.free.max.repos=1
specwatch.team.max.repos=10

# ===========================
# SECURITY
# ===========================
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-binding-errors=never